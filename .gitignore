/watch-your-ai-code
# `make release` output: per-platform tar.gz uploaded to the GitHub release.
/dist/
frontend/node_modules/
# `npm run build` output, embedded by backend/main.go via go:embed.
backend/internal/web/dist/
.DS_Store
# `make dev` scratch: air's rebuilt binary + the dev server's own board/library.
/.dev/
